# Docker Compose Environment Variables
# Copy this file to .env.docker and update with your values

# GitHub Container Registry
# Format: owner/repository (e.g., luz/zimevents-web)
GITHUB_REPOSITORY=luz/zimevents-web

# Image tag to pull (default: latest)
# Options: latest, master-<commit-sha>, or custom tag
IMAGE_TAG=latest